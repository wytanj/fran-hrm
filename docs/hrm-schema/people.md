# FranHRM people schema (git source)

This file is generated by `npm run schema:dump` from `core/staff/fields.mjs`, `core/permissions/catalog.mjs` and `core/hrm-schema/invariants.mjs`. Do not edit it by hand.

```
FranHRM people schema — in force
Identifier: fran-hrm.people  ·  format 1  ·  SG  ·  SGD

This is the human record. Dates are YYYY-MM-DD on the Asia/Singapore calendar; weeks start monday. Money is integer_cents.

TITLES
  Formal (payroll, contracts): positions.title
  Comms (what we say): staff.comms_title || positions.comms_title
  Display: comms || formal

HIERARCHY
  Designed line: positions.reports_to_id
  Person override: staff.reports_to_id
  Resolve order: staff.reports_to_id → holder_of(seat.reports_to_id)
  Vacant or shared manager seat: report_gap_do_not_guess

ACCOUNTABILITY
  One owner, required. Owner attaches to a seat first, with an optional person pin. Contributors stay on their own table.

CITIZENSHIP (staff.residency)
  - citizen: Singaporean
  - pr: PR
  - foreigner: Foreigner

RACE (Self-Help Group fund)
  - chinese: Chinese
  - malay: Malay
  - indian: Indian
  - eurasian: Eurasian
  - other: Other

EMPLOYMENT
  Types: Full-time, Part-time, Contractor
  Status: Active, Inactive, Terminated
  Access roles: Staff, Supervisor, Store Manager, Area Manager, Finance, HQ Admin
  Offboarding: real staff are terminated; only dummies may be purged.

DEPARTMENTS
  Many-to-many via staff_departments → org_functions. Primary flag: is_primary. If empty, infer seat.function.

STAFF FIELDS (built-in)
  [Identity]
    display_name (text, directory) required
    employee_code (text, directory)
    gender (enum, directory) ∈ {female, male, non_binary, prefer_not_to_say, other}
    date_of_birth (date, pii)
  [Statutory (Singapore)]
    nric (text, pii) — Singapore NRIC or FIN. Sensitive.
    race (enum, pii) ∈ {chinese, malay, indian, eurasian, other} — Drives the Self-Help Group fund on the CPF EZPay file.
    residency (enum, pii) ∈ {citizen, pr, foreigner} — Singaporean, PR or foreigner.
    nationality (text, pii)
    pr_start_date (date, pii) — Required for PR so CPF can pick year 1 / year 2 / year 3.
    cpf_applicable (boolean, pii)
  [Employment]
    role (enum, directory) ∈ {staff, supervisor, store_manager, area_manager, finance, hq_admin}
    employment_type (enum, directory) ∈ {full_time, part_time, contractor}
    employment_status (enum, directory) ∈ {active, inactive, terminated}
    hired_on (date, directory)
    terminated_on (date, directory)
    home_store_id (store_ref, directory)
    access_method (enum, directory) ∈ {pin, sso, otp}
    pos_access_enabled (boolean, directory)
    pt_weekly_hour_cap (number, directory)
    pt_monthly_hour_cap (number, directory)
  [Org & hierarchy]
    position_id (position_ref, directory) — The designed org seat. Titles come from here.
    comms_title (text, directory) — Per-person outward title. Blank = use the seat's comms title.
    reports_to_id (staff_ref, directory) — Explicit manager. Blank = derive from the seat.
  [Contact]
    email (text, directory)
    phone (text, directory)
    emergency_contact_name (text, pii)
    emergency_contact_phone (text, pii)
  [Home address]
    address_line_1 (text, pii)
    address_line_2 (text, pii)
    unit_number (text, pii)
    postal_code (text, pii)
    country (text, pii)
  [Pay]
    monthly_salary_cents (money_cents, compensation) — Monthly basic, integer cents. For salaried / full-time.
    hourly_rate_cents (money_cents, compensation) — Hourly rate, integer cents. For part-time.
    bank_name (text, compensation)
    bank_account_no (text, compensation)

PEOPLE PERMISSIONS
  Sensitive pay/PII readable with: reports:cost or staff:write
    staff:read — View the staff directory
    staff:write — Create and edit staff records
    org:read — View org chart and accountabilities
    org:write — Edit accountabilities and seats
    reports:cost — See pay rates and manpower cost

INVARIANTS
  [two-title-forms] Every person has two titles. positions.title is internal/formal (payroll, contracts). positions.comms_title — or staff.comms_title if set — is what we say out loud. display_title is comms, falling back to formal. Never mix the two registers in one sentence.
  [hierarchy-seat-and-person] Reporting lines live on seats (positions.reports_to_id) AND optionally on the person (staff.reports_to_id). The person override wins, so an interim arrangement does not require rewriting the designed org. A vacant or multiply-held manager seat is reported, never guessed.
  [no-reporting-cycle] A reporting-line edit that would create a loop — including through a seat-derived manager — is refused. A loop makes the chart and every manager query unresolvable.
  [one-accountable-owner] Exactly one accountable owner per accountability. Owner attaches to a seat (survives turnover) with an optional owner_staff_id pin. The row may not have neither. A vacant or shared seat is a gap, not a fallback to a contributor.
  [contributors-are-separate] Contributors (contributor / consulted / informed) are a separate table from the owner. Collapsing them is how accountability becomes plural and therefore nobody's.
  [two-permission-layers] A scope answers what KIND of action. Actor identity answers WHOSE records. Both are required for personal data. A scope check alone is not enough. There is no hardcoded role floor for capability — the editable matrix is the rule.
  [write-means-others] *:write means acts on other people. Self-service (own availability, own correction, own leave) runs on the matching *:read plus the identity check. Do not grant a staff member a write scope to "fix" a 403 on their own record.
  [sensitive-pay-and-pii] Pay, race, citizenship, home address, NRIC and bank ride reports:cost or staff:write. A directory read (staff:read) never carries them.
  [money-is-integer-cents] Money is integer cents. Monthly salary and hourly rate are stored as monthly_salary_cents and hourly_rate_cents. The UI may show dollars; the record does not.
  [dates-are-sgt-calendar] Dates are YYYY-MM-DD on the Singapore calendar. Times are timestamptz. Weeks start Monday. roster.week_start is always a Monday.
  [citizenship-three-values] Citizenship (staff.residency) is one of citizen (Singaporean), pr, or foreigner. That is what CPF EZPay uses. Nationality is a separate free-text field.
  [real-staff-are-terminated] Real staff are terminated, never hard-deleted, so timesheets and the audit trail survive. Only is_dummy rows may be purged. Termination revokes POS access; re-activation does not restore it.
  [departments-are-functions] Departments are org_functions. A person may belong to several (staff_departments); is_primary is the home. If none are set, the profile infers the seat's function so the chart and the record agree.
  [workspace-id-on-every-query] Every query filters workspace_id. The app talks to Postgres as the service role; RLS is on with zero policies. Tenancy is enforced in code.
  [audit-every-mutation] Mutations write audit_events (before/after). Audit insert failure is logged, never thrown — a write must not roll back because the trail failed.
  [custom-fields-do-not-shadow] Workspace custom fields extend the staff record without a migration. Their key cannot shadow a built-in field. Built-in columns stay first-class because payroll, CPF and the org resolver query them.

End of people schema.
```
