{
  "name": "fran-hrm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FranHRM — HR, manpower scheduling and time & attendance for the Fran retail stack (HQ + store staff).",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "db:migrate": "node scripts/migrate.mjs",
    "db:migrate:status": "node scripts/migrate.mjs --status",
    "db:seed": "node scripts/seed.mjs",
    "db:seed:org": "node scripts/seed-org.mjs",
    "help:sync": "node scripts/sync-help.mjs",
    "help:check": "node scripts/sync-help.mjs --check",
    "mcp": "node mcp/src/index.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@supabase/supabase-js": "^2.98.0",
    "bcryptjs": "^3.0.2",
    "nuxt": "^4.3.1",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "postgres": "^3.4.5"
  }
}
